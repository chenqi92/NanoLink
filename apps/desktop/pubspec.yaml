name: desktop
description: "NanoLink Desktop - Multi-server agent monitoring"
publish_to: 'none'
version: 0.1.5

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0
  http: ^1.2.0
  shared_preferences: ^2.3.0
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # ^6.0.0 requires Dart SDK ^3.8.0

flutter:
  uses-material-design: true
  assets:
    - assets/
